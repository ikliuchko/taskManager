# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'Managing tasks' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Managing tasks
  pod 'PromiseKit'
  pod 'SwaggerClient', :path => './Managing tasks/DevelopmentPods/swift5-client'
  pod 'KeychainAccess'

end
